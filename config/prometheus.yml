global:
  scrape_interval: 15s
  evaluation_interval: 15s
  external_labels:
    monitor: 'anthropic-bedrock-proxy'

scrape_configs:
  - job_name: 'api-proxy'
    static_configs:
      - targets: ['api-proxy:8000']
    metrics_path: '/metrics'
    scrape_interval: 10s
